import React, {
  Component,
  PropTypes,
} from 'react';

class Gatekeeper extends Component {
  render() {
    return (
      <div>
        <h1>This will be the entry point to the "Getting past the Gatekeeper" script</h1>
      </div>
    );
  }
}

Gatekeeper.propTypes = {};
Gatekeeper.defaultProps = {};

export default Gatekeeper;
